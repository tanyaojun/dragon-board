# KPLViewer 升级分析进度

## 2026-06-19

- 完整读取项目规则与相关 skills。
- 阅读 `tools/KPLViewer` 的数据提供器、刷新链路、统计和展示代码。
- 实测同花顺接口及四只股票的字段和类型分布。
- 阅读 `D:/TDX外挂/THS大单棱镜/THS大单棱镜.html` 的请求和展示逻辑。
- 当前进入目标截图能力拆解阶段，尚未修改生产代码。
- 已生成浏览器能力地图并验证 `http://localhost:58950` 可访问。
- 已完成目标截图模块与当前数据合同的可行性分级，等待用户选择升级范围。
- 审计 `proxy-server` 的腾讯行情、同花顺涨停池与竞价信号缓存。
- 实测参考图股票 `002297` 的换手、量比、封单、开板和连板字段，确认多数缺口可补齐。
- 用户确认排除竞价、允许依赖代理，并要求整体重命名为 THSBigOrder。
- 用户确认采用三个来源明确的代理接口并行加载，不改旧 KPL 大单路由。
- 用户确认三路数据的字段职责、刷新节奏与降级行为。
- 用户确认界面布局与验收设计；正式规格已写入 `docs/kpl-viewer/2026-06-19-ths-big-order-design.md` 并完成自审。
- 实施计划已写入 `docs/kpl-viewer/2026-06-19-ths-big-order-implementation.md`，共 9 个 TDD/验收任务。
- 主代理自审修正了未跟踪 KPLViewer 无法自动进入 worktree、net48 不应使用 init-only 属性、最终暂存命令含占位符三类问题。
- 已派发两位只读 subagent，分别评审代理数据链与 WinForms 迁移/UI 链。
- 第一轮交叉评审发现 3 个 Critical、14 个 Important、4 个 Minor；已核验并修复缓存运行时、代理 envelope、请求合同、迁移、刷新竞态、net48、DPI、旧交互和动态验收等问题。
- 第二轮复核剩余 1 个阶段提交 Critical 和 4 个可执行性 Important；已补主力/pricechange fixture、取消处理、Task 8 暂存范围及完成判定。
- 用户要求继续，下一步提交两份修订文档并创建隔离 worktree。

## 2026-06-20

- 收到第二轮优化需求：评估 python-bridge 行情链、补底部热力图、修正黄白线语义/位置。
- 已定位图表根因：黄线为同花顺分时涨跌幅，白线为独立缩放的大单累计净额，两者当前不可直接比较。
- 已确认底部双层金额绘制尚无基于金额强度的热力映射。
- 已审计 python-bridge：基础 L1 快照字段较全，但无量比、无当日分钟历史接口；不建议本轮把分钟成交额直接切换过去。
- 正在用真实同刻数据验证同花顺 `pricechange` 的语义，并评估改由腾讯分钟价格/累计额生成白色价格线与黄色均价线。
- 已用真实响应确认同花顺 `pricechange` 为涨跌幅；发现当前 3000 端口不是 feature worktree 代理，准备改用独立端口核对腾讯分钟字段。
- 已在 3011 启动 feature 代理并完成真实量价核对，确认可直接生成标准白色分时价与黄色 VWAP；等待用户确认白线业务语义后再定稿。
